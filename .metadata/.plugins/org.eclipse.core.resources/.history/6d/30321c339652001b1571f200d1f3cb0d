package Main;

import Tetris.*;
import UI.*;

public class Main {
	public static boolean isRun=false;
	public static void main(String args[]) {
		Tetris t=new Tetris();
		UIManager uim=new UIManager(t);
		uim.start();
	}
}
